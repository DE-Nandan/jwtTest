1. install required node modules using command:

npm install

2. MongoDB needs to be installed as it is the chosen DB for our usecase

3. run on  http://localhost:3013/



This simple app contains authentication, autherization (using jwt) with upload and fetch images functionality
